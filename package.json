{
  "name": "sorb-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Reference app wiring showing how to consume the Sorb packages",
  "type": "module",
  "scripts": {
    "sorb": "sorb dev",
    "tokens": "style-dictionary build --config ./sd.config.js",
    "dev": "npm run tokens && vite",
    "build": "npm run tokens && vite build",
    "preview": "vite preview",
    "storybook": "storybook dev -p 6006",
    "test": "node --test sd/"
  },
  "dependencies": {
    "@sorb/leaf": "^0.1.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@sorb/juice": "^0.1.0",
    "@vitejs/plugin-react": "^4.3.0",
    "style-dictionary": "^4.0.0",
    "vite": "^5.4.0"
  }
}
