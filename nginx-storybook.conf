server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Storybook's built output is a static SPA (index.html + iframe.html + assets).
    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
